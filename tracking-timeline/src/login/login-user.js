class LoginUser {
    constructor(email, fullname, username, password) {
        this.email = email;
        this.fullname = fullname;
        this.username = username;
        this.password = password;
    }
}

module.exports = LoginUser;